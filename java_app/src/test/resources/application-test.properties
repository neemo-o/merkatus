# Aponta para o banco de teste local (ou o do CI)
db.oficial.url=jdbc:postgresql://localhost:5432/erp_oficial
db.oficial.username=postgres
db.oficial.password=postgres

# Desativa o banco de licenças no teste (não precisamos dele)
db.licencas.url=jdbc:postgresql://localhost:5432/erp_oficial
db.licencas.username=postgres
db.licencas.password=postgres